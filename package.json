{
  "name": "cuassistant",
  "version": "0.1.0",
  "private": true,
  "description": "Personal assistant. Today: cron-driven inbox cascade that creates MS365 To Do tasks for actionable mail. Built to grow more capabilities over time.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "scan": "tsx src/index.ts",
    "scan:dry": "DRY_RUN=1 tsx src/index.ts",
    "ms365-login": "tsx scripts/ms365-login.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --write \"src/**/*.ts\" \"*.{md,json}\"",
    "format:check": "prettier --check \"src/**/*.ts\" \"*.{md,json}\""
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "prettier": "^3.3.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
